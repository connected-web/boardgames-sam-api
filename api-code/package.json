{
  "name": "boardgames",
  "version": "1.0.5",
  "description": "An API for summarising boardgame play stats using AWS SAM.",
  "main": "app.js",
  "author": "SAM CLI",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.32.0",
    "@aws-sdk/node-http-handler": "^3.32.0"
  },
  "scripts": {
    "test": "standard && mocha tests/unit/",
    "test:integration": "standard && mocha --timeout=8000 --slow=4000 tests/integration/",
    "lint": "standard --fix"
  },
  "standard": {
    "env": {
      "mocha": true
    }
  },
  "devDependencies": {
    "axios": "^0.24.0",
    "chai": "^4.2.0",
    "mocha": "^6.1.4",
    "standard": "^14.3.1",
    "yaml": "^1.10.2"
  }
}
